availableVocabularyList:
input:
  - accessToken
  - username
  - endpoint
  - targetService
  - buildNumber
output:
  - plainText
  - json
# todo: custom data struct ?
customDataStructMapping:
  - mappingName: patternA
    description: custom mapping example
    schema: {myDataUser: $username, myAccessToken: $accessToken}
    vocabularyBinding:
      - username
      - accessToken